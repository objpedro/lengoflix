import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        padding: 20,
    },
    cabecalho: {
        fontSize: 20,
        marginBottom: 10
    },
    poster: {
        resizeMode: "cover",
        width: 100,
        height: 150,
        marginRight: 10,
        marginBottom: 20,
        borderRadius: 5,
    },
})

export default styles;